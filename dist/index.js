"use strict";
function calculateTax(age, tax = 80) {
    if (tax)
        return tax;
    if (age < 50)
        return "string";
    return "string";
}
calculateTax(5);
//# sourceMappingURL=index.js.map